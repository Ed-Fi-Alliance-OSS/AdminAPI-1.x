meta {
  name: Landing Page
  type: http
  seq: 1
}

get {
  url: {{API_URL}}
  body: json
  auth: none
}

tests {
  // pm.test("Status code is OK", function () {
  //     pm.response.to.have.status(200);
  // });
  // 
  // const response = pm.response.json();
  // 
  // pm.test("Response includes expected properties", function () {
  //     pm.expect(response).to.have.property("version");
  //     pm.expect(response).to.have.property("build");
  // });
  // 
}
